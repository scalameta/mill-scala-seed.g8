name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  g8Test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-java@v3
        with:
          java-version: 11
          distribution: 'temurin'
      - name: g8Test
        run: sbt g8Test
